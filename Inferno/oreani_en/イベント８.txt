	#Player[FREE]
	#ScriptVersion[2]

	script_enemy_main
	{
let step=0;
gra=1;
let char=8;

@Initialize()
	{
	SetLife(1);ForbidShot(true);ForbidBomb(true);
	LoadGraphic(imgBoss);//ドット絵の読み込み
	SetTexture(imgBoss);//読み込んだ画像を指定する
	CreateEventFromScript("Event");
	}

@MainLoop(){
	if(OnEvent==true){step=GetEventStep();
		if(step == 0){
		}
		if(step == 1){
		SetMovePosition02(cenX,minY+130,30);
		}
		if(step == 2){
		yield;
		}
		if(step == 3){
		VanishEnemy;
		}
	}
}

@DrawLoop{
Graphic;
}

@Finalize(){
	DeleteGraphic(imgBoss);//画像をメモリから解放
	ForbidShot(false);ForbidBomb(false);
}

	#include_function ".\include.txt"
}
	

	script_event Event
	{

	let imgSHEN=GetCurrentScriptDirectory~".\img\shencut.png";
	let imgRYU=GetCurrentScriptDirectory~".\img\ryucut2.png";
		@Initialize
		{
		LoadGraphic(imgSHEN);//自機カット読み込み
		LoadGraphic(imgRYU);//自機カット読み込み
		}
		
		@MainLoop
		{

			SetStep(0);

			SetStep(1);
			Wait(60);		

			SetChar(LEFT,imgSHEN);
			graphicLEFT(1);
			MoveChar(LEFT,BACK);
			SetChar(RIGHT,imgRYU);//右側キャラクタの画像を設定
			graphicRIGHT(7);
			MoveChar(RIGHT,FRONT);

			SetNameFromText(RIGHT,TEXT_BLUE,"リュー","嵐を呼ぶ神様");
			TextOutC(60,"\c[BLUE]あれ、シェンか\n
					何してるんだこんなところで");//文字描画

			SetStep(2);

			graphicLEFT(3);
			MoveChar(LEFT,FRONT);
			graphicRIGHT(8);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]…おれと勝負しろ！！");//文字描画

			MoveChar(LEFT,BACK);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[BLUE]断る\n
					おれはお前に構ってるヒマないんで");//文字描画

			graphicLEFT(5);
			MoveChar(LEFT,FRONT);
			graphicRIGHT(4);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]せっかく見つけたのにひどい！！\n
					じゃあお前は何してるんだよ！！");//文字描画

			graphicLEFT(1);
			MoveChar(LEFT,BACK);
			graphicRIGHT(3);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[BLUE]地獄の図書館で勉強しようと思って\n
					それじゃ、またな");//文字描画

			graphicLEFT(3);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]またなー");//文字描画

			graphicLEFT(2);
			TextOut("\c[YELLOW]って、その手には乗らないぞ！！\n
					っていうか神様が地獄で勉強とか引くわー");//文字描画

			graphicLEFT(1);
			MoveChar(LEFT,BACK);
			graphicRIGHT(2);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[BLUE]天界じゃ集中できないんだよ！！\n
					主にお前のせいで！！");//文字描画

			TextOut("\c[BLUE]おれが集中してる時に限って\n
					ちょっかいかけやがって！！");//文字描画

			graphicLEFT(8);
			MoveChar(LEFT,FRONT);
			graphicRIGHT(6);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]そりゃお前の集中力が足りないからだ\n
					おれ悪くない");//文字描画

			DeleteMusic(GetCurrentScriptDirectory~"\bgm\8-1.wav");
			PlayMusic(GetCurrentScriptDirectory~"\bgm\8-2.wav");

			graphicLEFT(1);
			MoveChar(LEFT,BACK);
			graphicRIGHT(2);
			MoveChar(RIGHT,FRONT);
			TextOutC(40,"\c[BLUE]だー！！もう許さん！！\n
					もう怒ったからな！！本気だからな！！");//文字描画

			graphicLEFT(3);
			MoveChar(LEFT,FRONT);
			graphicRIGHT(2);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]なんだ、ノリいいじゃん");//文字描画


			MoveChar(LEFT,BACK);
			graphicRIGHT(2);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[BLUE]この小生意気な太陽野郎が！！\n
					今日こそお前に引導をわたしてやる！！");//文字描画

			SetStep(3);
			End();//最後にEnd()は必須
		}
		
		@Finalize
		{
		DeleteGraphic(imgSHEN);//自機カット読み込み
		DeleteGraphic(imgRYU);//自機カット読み込み
		}

function graphicLEFT(num){
	if(num==1){SetGraphicRect(LEFT,0,0,249,319);}//真顔
	else if(num==2){SetGraphicRect(LEFT,250,0,499,319);}//おこ
	else if(num==3){SetGraphicRect(LEFT,500,0,749,319);}//嬉しい
	else if(num==4){SetGraphicRect(LEFT,750,0,999,319);}//引き
	else if(num==5){SetGraphicRect(LEFT,0,320,249,640);}//びっくり
	else if(num==6){SetGraphicRect(LEFT,250,320,499,640);}//気づく
	else if(num==7){SetGraphicRect(LEFT,500,320,749,640);}//キラ
	else if(num==8){SetGraphicRect(LEFT,750,320,999,640);}//悪い顔
}
function graphicRIGHT(num){
	if(num==1){SetGraphicRect(RIGHT,0,0,249,319);}//照れ
	else if(num==2){SetGraphicRect(RIGHT,250,0,499,319);}//おこ
	else if(num==3){SetGraphicRect(RIGHT,500,0,749,319);}//嬉しい
	else if(num==4){SetGraphicRect(RIGHT,750,0,999,319);}//真顔
	else if(num==5){SetGraphicRect(RIGHT,0,320,249,640);}//悪い顔
	else if(num==6){SetGraphicRect(RIGHT,250,320,499,640);}//びっくり
	else if(num==7){SetGraphicRect(RIGHT,500,320,749,640);}//気づく
	else if(num==8){SetGraphicRect(RIGHT,750,320,999,640);}//焦る
}
	}
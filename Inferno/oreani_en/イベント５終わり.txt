

script_enemy_main
	{
let count = 0;
let step=0;
gra=1;
let char=5;


@Initialize()
	{
	SetLife(1);ForbidShot(true);ForbidBomb(true);
	SetGraphicRect(0,0,64,64);
	}

@MainLoop()
	{
	if(OnEvent()==true){break;}
	if(GetSpeed!=0){break;}
	if(count == 1)
		{}
	if(count == 2)
		{
		//SetMovePosition02(cx+1,145,80);
          	CreateEventFromScript("Event1end");
		}
	if(count == 3)
		{
		VanishEnemy;
		}
	count++;
	}

@DrawLoop{
Graphic;
}

@Finalize(){
	DeleteGraphic(imgBoss);//画像をメモリから解放
	ForbidShot(false);ForbidBomb(false);
}

#include_function ".\include.txt"
}
	

	script_event Event1end
	{

	let imgSHEN=GetCurrentScriptDirectory~".\img\shencut.png";
	let imgRYU=GetCurrentScriptDirectory~".\img\ryucut.png";
	let imgGALD=GetCurrentScriptDirectory~".\img\galdcut.png";

		@Initialize
		{
		LoadGraphic(imgSHEN);//自機カット読み込み
		LoadGraphic(imgRYU);//自機カット読み込み
		LoadGraphic(imgGALD);
		}
		
		@MainLoop
		{

			if(GetPlayerScriptName=="sh2.txt"){//シェン会話

			SetChar(LEFT,imgSHEN);//右側キャラクタの画像を設定
			graphicLEFT(7);
			MoveChar(LEFT,BACK);
			SetChar(RIGHT,imgGALD);//右側キャラクタの画像を設定
			graphicRIGHT(4);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[RED]強いじゃないか、気にいった！！\n
					今度カレーをご馳走してやる！！");//文字描画

			graphicLEFT(3);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]ごちそう！！ごちそう！！");//文字描画

			graphicLEFT(5);
			TextOut("\c[YELLOW]って、カレーってなんだ？\n
					それ心臓よりおいしいの？");//文字描画

			MoveChar(RIGHT,FRONT);
			MoveChar(LEFT,BACK);
			TextOut("\c[RED]心臓？\n
					ああ、心臓カレーもウマイぞ！！");//文字描画

			graphicLEFT(3);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]うおおおおお！！\n
					やる気出てきたー！！");//文字描画

			graphicLEFT(6);
			MoveChar(RIGHT,FRONT);
			MoveChar(LEFT,BACK);
			TextOut("\c[RED]…ってなことを言ってる間に到着だ\n
					魔王はそこを真っすぐ行くといるぞ");

			graphicLEFT(7);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[YELLOW]いよいよラスボスか…ゴクリ");//文字描画
			}


			if(GetPlayerScriptName=="ryu.txt"){//リュー会話

			SetChar(LEFT,imgRYU);//右側キャラクタの画像を設定
			graphicLEFT(1);
			MoveChar(LEFT,BACK);
			SetChar(RIGHT,imgGALD);//右側キャラクタの画像を設定
			graphicRIGHT(4);
			MoveChar(RIGHT,FRONT);
			TextOut("\c[RED]気にいった！！\n
					今度カレーをご馳走してやる！！");//文字描画

			graphicLEFT(2);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[BLUE]カレー？懐かしいなあ");//文字描画

			graphicLEFT(4);
			TextOut("\c[BLUE]じゃなくて、帰るったら帰る！\n
					帰ります！！");//文字描画

			graphicLEFT(7);
			MoveChar(RIGHT,FRONT);
			MoveChar(LEFT,BACK);
			TextOut("\c[RED]へへへ、もう遅い！！\n
					改めましてようこそ魔王殿へ！！");//文字描画

			graphicLEFT(5);
			TextOut("\c[RED]魔王はそこを真っすぐ行くといるぞ\n
					大丈夫、きっと歓迎してくれるからな！！");

			graphicLEFT(1);
			MoveChar(LEFT,FRONT);
			MoveChar(RIGHT,BACK);
			TextOut("\c[BLUE]そういう問題じゃないんだけど…\n
					仕方ない、魔王のところへ行くか");//文字描画
			}

			End();//最後にEnd()は必須
		}
		

function graphicLEFT(num){
	if(num==1){SetGraphicRect(LEFT,0,0,249,319);}//真顔
	else if(num==2){SetGraphicRect(LEFT,250,0,499,319);}//おこ
	else if(num==3){SetGraphicRect(LEFT,500,0,749,319);}//嬉しい
	else if(num==4){SetGraphicRect(LEFT,750,0,999,319);}//引き
	else if(num==5){SetGraphicRect(LEFT,0,320,249,640);}//びっくり
	else if(num==6){SetGraphicRect(LEFT,250,320,499,640);}//気づく
	else if(num==7){SetGraphicRect(LEFT,500,320,749,640);}//キラ
	else if(num==8){SetGraphicRect(LEFT,750,320,999,640);}//悪い顔
}
	
function graphicRIGHT(num){
	if(num==1){SetGraphicRect(RIGHT,0,0,250,320);}//真顔
	else if(num==2){SetGraphicRect(RIGHT,250,0,500,320);}//喜び
	else if(num==3){SetGraphicRect(RIGHT,500,0,750,320);}//爆笑
	else if(num==4){SetGraphicRect(RIGHT,750,0,1000,320);}//ボロ絵
}
		
		@Finalize
		{
		DeleteGraphic(imgSHEN);
		DeleteGraphic(imgRYU);
		DeleteGraphic(imgGALD);
		}
	}